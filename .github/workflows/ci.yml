# This starter workflow is for a CMake project running on a single platform. There is a different starter workflow if you need cross-platform coverage.
# See: https://github.com/actions/starter-workflows/blob/main/ci/cmake-multi-platform.yml
name: madbfs CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v4

    - name: Configure C++ Development Tools
      run: |
        sudo apt update
        sudo apt install gcc-14 g++-14 cmake ninja-build git

    - name: Configure Conan 2 Package Manager
      run: |
        sudo apt install python3 python3-pip
        pip3 install conan
        conan profile detect --force

    - name: Configure Android NDK
      run: |
        wget -nv https://dl.google.com/android/repository/android-ndk-r29-beta1-linux.zip
        unzip -q android-ndk-r29-beta1-linux

    - name: Build, Test, and Package
      run: ./package.sh
      env:
        ANDROID_NDK_HOME: android-ndk-r29-beta1-linux

# todo: add filesystem test job (requires android emulation to exist)
